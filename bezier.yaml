name: Bézier
identifier: it.macteo.bezier
# resources: assets # Optional
# cover: cover.jpeg # Optional
deployment_target: ios10.0
imports:
 - UIKit
chapters:
  - name: Bézier
    live_view_mode: "VisibleByDefault"